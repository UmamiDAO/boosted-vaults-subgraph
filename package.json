{
  "name": "boosted-glp-vaults-pre-live",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:remote": "graph codegen && graph build && graph deploy --product hosted-service umamidao/boosted-glp-vaults-pre-live",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
